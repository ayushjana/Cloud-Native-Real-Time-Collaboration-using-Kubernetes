apiVersion: v1
kind: Secret
metadata:
  name: chat-app-secret
  namespace: chat-app
type: Opaque
stringData:
  JWT_SECRET: "your-secret-key-change-this-in-production-12345"
  # Note: In production, use kubectl create secret or sealed-secrets
  # kubectl create secret generic chat-app-secret --from-literal=JWT_SECRET=your-actual-secret

